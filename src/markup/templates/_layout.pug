include _mixins.pug

block variables
  
doctype html
html(lang='ru')
  +dev_head(page_title || '', scripts || [])

  body
    .wrapper
      .base
        +header()
        
        main.main__content
          block content
            
      +footer()
      
    +scripts(true)
